<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- WebRTC audio engine (VOICE_COMMUNICATION capture + hardware AEC/NS) needs
         to manage audio mode + effects for echo cancellation. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Haptic listening on/off cue (HapticCue) — replaces the beep that collided
         with TTS on the shared WebRTC voice path. Normal permission, no prompt. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- OTA self-update (docs/OTA.md §5): install its own downloaded APK. As a
         device-owner the install is silent; otherwise a system confirm dialog. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.microphone" android:required="true" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:name=".DockApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NodeDock"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- singleTask: the dock face must never STACK — a second launch (adb,
             notification tap, launcher) re-surfaces the existing instance.
             Stacked copies ran two agents/wirings: every utterance produced
             two competing turns + replies (seen live). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.NodeDock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FaceGalleryActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:theme="@style/Theme.NodeDock" />

        <service
            android:name=".service.PerceptionService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="microphone" />

        <!-- Device-admin receiver for OTA (docs/OTA.md §5.1). Provisioned ONCE
             via `adb shell dpm set-device-owner dev.orbit.dock/.ota.DockDeviceAdminReceiver`
             on a fresh phone; being device-owner unlocks SILENT PackageInstaller
             updates (no per-install tap). Harmless if never provisioned — the
             updater falls back to the system confirm dialog. -->
        <receiver
            android:name=".ota.DockDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Relaunch the dock after an OTA self-update (MY_PACKAGE_REPLACED) or a
             phone reboot (BOOT_COMPLETED). After a self-update Android kills us
             without relaunching; this receiver runs in the fresh post-update
             process and starts the app back up. See docs/OTA.md §5. -->
        <receiver
            android:name=".ota.RelaunchReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
