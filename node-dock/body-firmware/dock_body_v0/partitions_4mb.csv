# Dual-slot OTA partition table for the dock body on a 4 MB flash
# (ESP32-C3 mini). Mirrors partitions.csv (the 8 MB / S3 table) but with
# OTA slots sized to fit 4 MB. See docs/OTA.md §4.1.
#
# Two app slots (ota_0/ota_1) + otadata lets the bootloader flip between a
# running image and a freshly-OTA'd one, with rollback to last-good if the
# new image fails to validate (§4.3).
#
# Budget (4 MB = 0x400000):
#   nvs/otadata/phy + bootloader/table headroom ≈ 0x20000
#   → two 0x1E0000 (1.875 MB) slots fit with margin. The firmware is well
#     under 1 MB, so this leaves generous OTA growth room.
#
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
otadata,    data, ota,     0xf000,   0x2000
phy_init,   data, phy,     0x11000,  0x1000
ota_0,      app,  ota_0,   0x20000,  0x1E0000
ota_1,      app,  ota_1,   0x200000, 0x1E0000
